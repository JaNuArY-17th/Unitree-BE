export declare const TOKEN_EXPIRATION_TIME: {
    ACCESS_TOKEN: number;
    REFRESH_TOKEN: number;
};
export declare enum TokenPrefixes {
    USER = "users:"
}
export declare enum UserHashFields {
    PROFILE = "profile",
    REFRESH_TOKENS = "refresh_tokens",
    GARDEN_STATE = "garden_state"
}
